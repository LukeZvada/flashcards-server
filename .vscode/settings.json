{
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "flash",
            "database": "${workspaceFolder:flashcards-server}/db.sqlite3"
        }
    ]
}